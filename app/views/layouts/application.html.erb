<!DOCTYPE html>
<html>
<head>
  <title>Notes</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

<meta charset="utf-8" />
<link href="http://fonts.googleapis.com/css?family=Reenie+Beanie:regular" rel="stylesheet" type="text/css"> 
<style type="text/css">
	body{
	  margin:0;
	  padding:0;
	  font-family:arial,sans-serif;
	  font-size:100%;
	  margin:3em;
	  background:#666;
	  color:#fff;
	}
	h2,p{
	  margin:0;
	  padding:0;
	  font-size:100%;
	  font-weight:normal;
	}
	ul,li{
	  list-style:none;
	}
	ul{
	  overflow:hidden;
	  padding:1em;
	}
	ul li a{
	  margin:0;
	  padding:0;
	  text-decoration:none;
	  color:#000;
	  background:#fffc73;
	  /* creates a block with 10em x 10em */
	  display:block;
	  height:10em;
	  width:10em;
	  padding:1em;
	  
	  /* Box Shadow to create the shadow effect for Firefox, WebKit & Opera */  
	  -moz-box-shadow:5px 5px 7px rgba(33,33,33,.7);
	  -webkit-box-shadow: 5px 5px 7px rgba(33,33,33,.7);  
	  box-shadow: 5px 5px 7px rgba(33,33,33,.7); 

	  /* Transform to tilt the notes */
	  -webkit-transform:rotate(-6deg);  
	  -o-transform:rotate(-6deg);  
	  -moz-transform:rotate(-6deg);  

	  /* Transform to create a smooth transition in time for the tilting effect of the notes */
	  -moz-transition:-moz-transform .15s linear;  
	  -o-transition:-o-transform .15s linear;  
	  -webkit-transition:-webkit-transform .15s linear;  
	}
	ul li:nth-child(even) a{
	  -o-transform:rotate(4deg);  
	  -webkit-transform:rotate(4deg);  
	  -moz-transform:rotate(4deg);  
	  position:relative;  
	  top:5px; 
	  background:#e5399e;
		}
	ul li:nth-child(3n) a{
	  -o-transform:rotate(-3deg);  
	  -webkit-transform:rotate(-3deg);  
	  -moz-transform:rotate(-3deg);  
	  position:relative;  
	  top:-5px;  
	  background:#ccf;
	}
	ul li:nth-child(5n) a{  
	  -o-transform:rotate(5deg);  
	  -webkit-transform:rotate(5deg);  
	  -moz-transform:rotate(5deg);  
	  position:relative;  
	  top:-10px;  
	  background:#aef26d;
	}  
	ul li a:hover, ul li a:focus {  
	  /* Transform to create a smooth shadow & zoom effect on focus and hover over on the notes */
	  -moz-box-shadow:10px 10px 7px rgba(0,0,0,.7);  
	  -webkit-box-shadow: 10px 10px 7px rgba(0,0,0,.7);  
	  box-shadow:10px 10px 7px rgba(0,0,0,.7);  

	  -webkit-transform: scale(1.25);  
	  -moz-transform: scale(1.25);  
	  -o-transform: scale(1.25);  
	  position:relative;  
	  z-index:5; /* brings the focused note to front */ 
	}  
	ul li{
	  margin:1em;
	  float:left;
	}

	ul li h2{  
	  font-size:120%;  
	  font-weight:bold;  
	  padding-bottom:10px;  
	}  
	ul li p{  
	  margin:0;
	  padding:0;
	  font-family:"Reenie Beanie",arial,sans-serif;  
	  font-size:200%;  
	}  
</style>
</head>
<body>

<%= yield %>

</body>
</html>
